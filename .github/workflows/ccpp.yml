name: C/C++ CI

on:
  push:
    # branches: [ master ]
  pull_request:
    # branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v2
    
    # in server 
    - name: install_lib
      run: |
        sudo apt-get install cmake gcc lcov cppcheck python3 python3-pip valgrind flawfinder doxygen graphviz python3-sphinx libgtk-3-dev
    #- name: ws
    #  run: ls -al ; pwd ;
    # in git dir
    - name: update_pip
      run: |
        pip install -r ./docs/requirements.txt
    - name: compil
      run: |
        ls ../

    # mkdir build ; cd build
    #    cmake ../ 
    #- name: make
    #  run: make
    #- name: make test
    #  run: make test
    #- name: make covrage
    #  run: make coverage
      
      
